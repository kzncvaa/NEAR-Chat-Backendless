To get started follow the instructions below:
1. Run the following command to install React and Backendless Node modules:
npm install
2. Run the following command to start React server and open the app in the browser:
npm start